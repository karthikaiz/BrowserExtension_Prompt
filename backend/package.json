{
  "name": "prompt-enhancer-api",
  "version": "1.0.0",
  "description": "AI-powered prompt enhancement API",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "start": "echo 'Use vercel dev for local development'"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  },
  "engines": {
    "node": ">=18"
  }
}